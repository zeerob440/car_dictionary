# create a function that that sets make, model and color.
# the function should create a dict that has the correct setting and contains the functions already created
# the function should return a dict so users can assign any new car they want to it. 
